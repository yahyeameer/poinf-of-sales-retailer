# Normalise to LF in the repo. Contributors will be on Windows, macOS and Linux,
# and SQL migrations that flip line endings produce unreadable diffs.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
